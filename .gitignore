/public/
/resources/
.hugo_build.lock
.claude/settings.local.json

# oryginalne nagrania - za duże na repo, trzymane tylko lokalnie
raw/*.mov
raw/*.mp4
raw/*.m4a

# surowe transkrypcje - tylko lokalnie, źródło dla postów
transcripts/
